import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'linkedin-clone-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
